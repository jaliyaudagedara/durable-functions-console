namespace DfxConsole.BlazorUI.Constants;

public enum DurableOperation
{
    GetInstances,
    GetInstance,
    PurgeInstance,
    PurgeInstances,
    TerminateInstance,
    SuspendInstance,
    ResumeInstance,
    GetEntities,
    GetEntity
}